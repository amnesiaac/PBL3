@model IEnumerable<PBL3.Models.AbastecimentoBebedouro>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Bebedouro.Localizacao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estoque.EstoqueId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Bebedouro.Localizacao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estoque.EstoqueId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AbastecimentoBebedouroId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AbastecimentoBebedouroId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AbastecimentoBebedouroId })
        </td>
    </tr>
}

</table>
